pi5/venv/
pi5/.env
.env
__pycache__/
*.py[cod]
.pytest_cache/
frontend/node_modules/
frontend/dist/

# Vite production UI. npm run build writes here; FastAPI serves it.
# Keep pi5/static/ in git so a Pi checkout does not need Node.js.
!pi5/static/
!pi5/static/**
